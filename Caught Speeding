public int caughtSpeeding(int speed, boolean isBirthday) {
  if(isBirthday==true){
    if(speed<66) return 0;
    if(speed<86) return 1;
    if(speed<87) return 2;
  }
  if(speed<=60) return 0;
  if(speed>=61 && speed<=80) return 1;
  if(speed>=81) return 2;
  return 0;
}
